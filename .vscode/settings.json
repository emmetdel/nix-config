{
    "nixEnvSelector.nixFile": "${workspaceFolder}/shell.nix",
    "nix.enableLanguageServer": true,
    "nix.serverPath": "nil",
    "nix.serverSettings": {
        "nil": {
            "enable": true,
            "formatting": {
                "command": ["alejandra"]
            }
        }
    }
}